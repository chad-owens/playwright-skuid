To run tests for taco admin console:

`Clone repo`

From command line:

`npm install`

`SKUID_HOST=https://titanium.skuidsite.ink ./node_modules/.bin/playwright test --project=admin --config=tests/tacos --headed`
